# pnpm configuration
# Allow all build scripts to run
enable-pre-post-scripts=true

# Use hoisted node_modules structure for Electron compatibility
# This is required because Electron's require('electron') must resolve
# to the built-in module, not the npm package
node-linker=hoisted
shamefully-hoist=true
